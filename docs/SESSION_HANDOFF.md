# Session Handoff

Use this file to resume work without re-deriving context.

## Current State

- **Phase:** 3 — Features (Phase 2 — UI is COMPLETE, gate passed)
- **Current module:** 3.9 — Hosted Landing Page QR Types (not yet started)
- **Last completed module:** 3.8 — File-Based QR Types and Supabase Storage (COMPLETE)
- **Branch/commit:** local git repo (`master`). Check `git log --oneline -20` for the actual latest commit when resuming — as of writing, Modules 1.1–3.7, the account-entitlements infrastructure, and the dynamic-QR-quota extension are committed; Module 3.8 is ready to commit.
- **Supabase integration status:** **Live and connected.** Project URL + anon key are in `.env.local` (gitignored). Schema (14 migrations — Module 3.8 added `qr_asset_is_publicly_readable` + new `storage.objects` read policies, and `resolve_landing_page`) matches `supabase/migrations/`. Real auth (Module 3.1), real QR persistence (Module 3.5), real dynamic-QR redirects (Module 3.6), real scan analytics (Module 3.7), a real RLS-locked plan-entitlement system + dynamic-QR quota, and now **real file-based QR types with RLS-only public Storage access** — see below. `SUPABASE_SERVICE_ROLE_KEY` is still blank and not needed anywhere yet.
- **QR rendering status:** Fully real, fully styled, and fully exportable — see Modules 3.2–3.4. Unchanged since Module 3.6.
- **QR persistence status (Module 3.5, unchanged)**: Save/Update/Duplicate/Archive/Delete all real, RLS-enforced, ownership from the session only (`src/lib/qr/actions.ts`). Dashboard overview/list/detail/edit pages all use real data (`src/lib/qr/queries.ts`).
- **Dynamic QR status (Module 3.6, unchanged)**: `/r/[slug]` (`src/app/r/[slug]/route.ts`) is real — resolves via `resolve_qr_redirect` RPC, redirects (307) or rejects (404 not_found / 410 inactive). `destination_url` populated on save/update/duplicate for `url`/`whatsapp` dynamic QRs. Pause/Reactivate wired into `QRCodeRowActions`.
- **Scan analytics status (Module 3.7, unchanged since)**: `record_qr_scan` (called via `after()`, still non-blocking) records real `device_type`/`os`/`browser` (parsed from User-Agent, `src/lib/qr/user-agent.ts`) and `country_code` (only when a hosting edge header provides one — `x-vercel-ip-country`/`cf-ipcountry`, never a geo-IP call). `src/lib/qr/queries.ts`'s `listScanEvents()` feeds the real analytics page. `AnalyticsView` hides the entire Country UI when no event has one. Live-verified against the real project — see `docs/ARCHITECTURE.md`'s Module 3.7 section.
- **Account entitlements**: `account_entitlements` table — RLS allows owner-only `select`, **no write policy for any role at all**, so a plan can only be granted/changed via a direct privileged database operation, never from the client. `getMyEntitlement()`/`planLabel()` (`src/lib/account/entitlements.ts`) feed a small real "Plan" card on `/dashboard/account`. A real, permanent account (`mts.pk@hotmail.com` — Lifetime Pro owner) exists in the live project with `plan='pro'`, `is_lifetime=true`, `expires_at=null` — **do not delete this account or its entitlement row in any future cleanup**; it is not test data. Its password lives only in Supabase Auth, never in this repo.
- **Dynamic QR quota (unchanged since)**: `account_entitlements.dynamic_qr_limit` (nullable, `NULL` = unlimited). `resolveDynamicQrAllowance()`/`checkDynamicQrAllowance()` gate `saveQrCode`/`updateQrCode`(mode-conversion only)/`duplicateQrCode` server-side. The permanent account's `dynamic_qr_limit` is explicitly `null`. Full rationale: `docs/ARCHITECTURE.md`'s "Dynamic QR Quota" section.
- **File-based QR types (new, Module 3.8)**: `pdf`/`images`/`audio`/`video` are real now — real Storage upload (`src/lib/qr/asset-upload.ts`, direct browser→Storage via the owner's own session), real `qr_assets` sync on save/update/delete (`src/lib/qr/asset-sync.ts`), and a real `/p/[slug]` landing page (`src/app/p/[slug]/page.tsx`, `src/components/landing/`). Public read access to a file is granted **without a service-role key** — a `SECURITY DEFINER` function (`qr_asset_is_publicly_readable`) embedded inside new `storage.objects` RLS policies, scoped to `dynamic`+`active` QRs only, auto-revoked the instant a QR is paused/archived. A second `SECURITY DEFINER` function, `resolve_landing_page`, mirrors `resolve_qr_redirect` for slug resolution. Files page (`/dashboard/files`) now reads/deletes real `qr_assets` rows (`src/lib/files/{queries,actions}.ts`). Full design rationale and the complete live-verification record (PDF/Gallery/Audio/Video, all four fully driven through the real Server Actions and cleaned up): `docs/ARCHITECTURE.md`'s "File-Based QR Types and Supabase Storage" section. **One gap**: the Files page's own list/delete path wasn't separately browser-driven this pass (no `SUPABASE_ACCESS_TOKEN` available this session to provision a confirmed test account — see below); covered instead by 8 passing unit tests over the identical logic.
- **Test status:** 347 unit/component tests passing across 55 files. `typecheck`, `lint` (0 errors, 9 pre-existing warnings), `format:check`, and a fresh production `build` all pass.

## Relevant Commands

```bash
npm install
npm run dev          # http://localhost:3000
npm run build
npm run start         # production server
npm run typecheck
npm run lint
npm run format:check
npm run test

# Local Supabase (Docker required, fully offline, no account needed — still used for schema
# iteration/testing before pushing to the live project):
supabase start        # reuses existing DB volume; does NOT auto-apply new migrations
supabase db reset     # drops + recreates from supabase/migrations/*.sql + seed.sql
supabase stop

# Live project (now linked — supabase/.temp/ holds the link, gitignored):
supabase db push --linked      # push new migrations to the live project
supabase db query --linked -o json   # ad-hoc read-only SQL against the live project
```

## Current Blockers

None. Module 3.9 doesn't need anything from the user to start.

One open loose end from Module 3.8, not blocking: `SUPABASE_ACCESS_TOKEN` wasn't available in this session's shell (it's never persisted — see the note below), so the Files page's own `listQrAssets`/`deleteQrAsset` path couldn't be driven through a fresh confirmed-account browser session this pass (a normal signup requires email confirmation, and there was no way to auto-confirm it). A harmless unconfirmed signup, `qrforge.module38.filesverify@gmail.com`, was left in `auth.users` as a result — it can never authenticate or own data since it's unconfirmed, but a future session with `SUPABASE_ACCESS_TOKEN` available could either use it (toggle `mailer_autoconfirm`, confirm it, run the check, delete it) or just delete the row outright.

## ⚠️ Read before doing browser-based verification

**The Browser pane's reliability for interaction testing varies within the same session — verify before trusting it, don't assume either way.** In Module 3.1, `read_page`/`screenshot`/`computer` clicks worked for the login form (a real login → dashboard → logout cycle was driven and confirmed this way). Later the same session, for Module 3.2's generator page, `read_page`/`screenshot` returned empty/timed out again (the pane-compositing limitation from earlier in this project), and — notably — a scripted workaround (native `HTMLInputElement` value setter + dispatched `input` event, the classic React 16-era trick for faking user input) did **not** reach React Hook Form's `watch()` subscription in this environment; root cause unconfirmed, possibly a React 19 internals difference. `get_page_text`, `read_network_requests`, and `javascript_tool` reads (not simulated writes) kept working throughout both cases.

**Practical guidance**: try the Browser pane, but check early (e.g. `read_page` right after navigating) whether it's actually compositing this session before relying on it for a multi-step interaction. If it isn't, don't fight it with JS-injection workarounds — fall back to the established, always-reliable methods: Vitest + Testing Library component tests (using real `userEvent`, which drives actual browser-equivalent input events through jsdom more faithfully than a hand-rolled script) for interactive logic, and `curl` against the dev/production server for route-level content, redirect, and status checks.

**A second, unrelated live-verification constraint hit in Module 3.5**: the live Supabase project has a project-wide email send rate limit (shared free-tier SMTP). After a few confirmation emails in one session (creating throwaway test accounts for RLS/browser verification), further signups return `429 over_email_send_rate_limit` — this blocks provisioning _new_ confirmed test accounts, separate from the Browser-pane issue above. If you hit this: don't retry in a loop (respect the platform's limit); either wait, reuse an existing still-valid test session within the same run if one exists, or document the gap honestly per the master prompt's own instruction to prefer strong component/integration evidence over a skipped/faked manual check.

**Module 3.6's workaround for the above, reusable going forward**: rather than hit the email rate limit again, temporarily flip `mailer_autoconfirm` to `true` via the Management API (`PATCH https://api.supabase.com/v1/projects/{ref}/config/auth` with `Authorization: Bearer $SUPABASE_ACCESS_TOKEN`), sign up one throwaway account (immediate session, no email sent at all), do the verification, delete the account, then flip `mailer_autoconfirm` back to `false` before finishing. Confirm the restore actually took (re-read the config) rather than assuming the PATCH succeeded. This avoids the rate limit entirely instead of working around it.

**Also from Module 3.6**: `supabase db query --linked "<sql>"` mangles double-quotes inside inline JSON literals passed as a shell argument (bash escaping collides with the CLI's own parsing) — write the SQL to a scratch file and use `--file <path>` instead, which is reliable. `db query` output only surfaces the **last** statement's result set when multiple statements are sent in one call — if you need results from more than one statement, run them as separate calls.

**Two testing lessons banked from Phase 2**, still relevant:

1. Don't wait on real `setTimeout`-based delays via `findByText`/`findByRole` — assert on the synchronous state transition instead.
2. Write the test asserting the _specific, correct_ behavior before assuming an implementation is right — several real bugs (Module 2.4's `forwardRef` gap, Module 2.6's active-link double-match) were only caught this way.

**Mocking Supabase in component tests (new, Module 3.1)**: `vi.mock("@/lib/supabase/client", () => ({ createClient: () => ({ auth: { <method>: vi.fn(...) } }) }))`, plus `vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn(), refresh: vi.fn() }) }))` for any component that redirects after a successful call. For a "still submitting" assertion, mock the Supabase call to return a promise that never resolves (`() => new Promise(() => {})`) rather than depending on timing — see `LoginForm.test.tsx`/`SignupForm.test.tsx`/etc. for the reference pattern.

**A latent-bug pattern from Module 2.5, still relevant**: a plain (non-`forwardRef`) component silently accepts `{...someProps}` containing a `ref` without a `tsc` error (spread bypasses excess-property checking), but an explicit `ref={x}` on the same component does error. Keep any new form-field primitive `forwardRef`-wrapped.

## Next Exact Task

Start Module 3.9 (Hosted Landing Page QR Types) — re-read its full section in `QR_Code_Generator_Master_Build_Prompt.md` first.

1. **This extends Module 3.8's `/p/[slug]` infrastructure, it does not build a second one.** `resolveLandingPage`/`resolve_landing_page` (`src/server/services/landing-page-resolution.ts`, the RPC in `supabase/migrations/20260819180100_add_resolve_landing_page.sql`) and the type-switch in `src/app/p/[slug]/page.tsx` already exist — this module adds new cases to that switch (`src/components/landing/`) and new content forms, not a parallel resolver or route.
2. **Four remaining `notYetImplementedQrSchema` types**: `social`, `multi_link`, `menu`, `feedback` (check `src/lib/qr/registry.ts` for the exact current set — these are the ones still flagged `needsLandingPage: true` with no real schema after Module 3.8). Unlike Module 3.8's file types, these are pure structured data (links, menu items, a feedback form's own field config) — no Supabase Storage involved, so this is a `payload_data` design, not `landing_page_config` (confirm which column the master prompt actually intends before writing the payload builders; `landing_page_config` has sat unused since Module 1.4, specifically earmarked for this module).
3. **Feedback is the one type in this batch with real write-back**: a visitor submitting a feedback form needs an insert path with no authenticated session — the same "narrow `SECURITY DEFINER` RPC keyed by slug" pattern from `record_qr_scan`/Module 3.6 is the template, not a client-facing insert policy on a new table (mirror the reasoning in `docs/ARCHITECTURE.md`'s Module 3.6 section).
4. Verify (typecheck/lint/format/build/vitest, then live against the real Supabase project with a throwaway account, cleaned up afterward), document in `docs/ARCHITECTURE.md`, mark Module 3.9 complete in `docs/WORKLOG.md`, commit. Continue autonomously into Module 3.10 afterward per the standing instruction, unless a genuine blocker (credential, destructive decision, schema conflict) arises.
5. **If live verification needs a new confirmed test account**: `SUPABASE_ACCESS_TOKEN` is not available by default (see the note below) — either ask the user for it up front if the `mailer_autoconfirm` toggle will be needed, or check whether Supabase's email-send rate limit has reset enough to use a real signup with a real inbox instead.

## Notes for Future Sessions

- **⚠️ `mts.pk@hotmail.com` in the live Supabase project is a real, permanent Lifetime Pro account — not test data.** Any future live-verification cleanup (`delete from auth.users where ...`, or similar) must exclude this account explicitly. Its `account_entitlements` row (`plan='pro'`, `is_lifetime=true`, `expires_at=null`, `dynamic_qr_limit=null`) must also never be modified except by an explicit, deliberate future instruction to change the entitlement. See `docs/ARCHITECTURE.md`'s "Account Entitlements" and "Dynamic QR Quota" sections for how it was created and verified.
- **Live-verifying a Server Action when the Browser pane isn't compositing (new technique, banked from the dynamic-QR-quota work)**: rather than fake a manual UI click-through, add a temporary, uncommitted Route Handler (e.g. `src/app/api/test-only-<name>/route.ts` — **not** prefixed with `_`, which Next treats as a private, unrouted folder) that imports and calls the Server Action directly, then hit it with `fetch()` from any page that _does_ render in the pane (same-origin, so the browser's real session cookie is sent automatically). This runs inside a genuine Next.js request context, so `cookies()`/RLS/the real session all apply exactly as they would from the real UI. Delete the route(s) before committing and confirm via `git status` that no trace remains.
- Repo root is `D:\AntiGravity\QR`. Next.js app at repo root (not a subfolder).
- Next.js 16.3.0 — check `node_modules/next/dist/docs/` before assuming v15-era behavior. `params`/`searchParams`/`cookies()`/`headers()` are all async.
- **Tailwind v4** (`^4.3.3`), CSS-first `@theme` in `src/app/globals.css`. This project's `--radius-sm/md/lg` override Tailwind's own built-in radius scale of the same names.
- **Design system**: primary `#0F766E`. Use the `src/components/ui/` primitives (`Button`/`buttonVariants()`, `Card`, `Input`/`Textarea`/`Select`/`FormField`/`PasswordInput`/`Alert`/`AccordionItem`) rather than raw markup.
- **Three route-group layouts**: `(marketing)` (Header/Footer), `(auth)` (centered `AuthCard`), `(dashboard)` (Sidebar + mobile drawer, now with a real `getAuthenticatedUser()` guard and a working Log Out control). Don't duplicate any of their chrome inside individual pages.
- **Auth is real now (Module 3.1)** — `src/lib/supabase/{client,server,dal,profile}.ts` + `src/proxy.ts` + `src/lib/supabase/actions.ts` (`logout()`). `proxy.ts` does the optimistic cookie check; `getAuthenticatedUser()` (DAL) is the mandatory database-verified re-check called from `(dashboard)/layout.tsx` — don't remove either half, they're deliberately layered per Next.js's own auth guidance, not redundant.
- **QR persistence is real now (Module 3.5)** — `src/lib/qr/{records,queries,actions,action-types,slug,draft-storage}.ts`. Read `docs/ARCHITECTURE.md`'s Module 3.5 section before touching any of these; in particular, `actions.ts` has a top-level `"use server"` directive, which means it **may only export async functions** — any new constant/type export belongs in `action-types.ts` instead, or the production build fails opaquely ("module has no exports") in a way typecheck/lint won't catch. Always run a real `npm run build` after touching Server Action files, not just typecheck.
- **Mock data remaining**: `src/lib/qr/mock-data.ts` and `src/lib/files/mock-data.ts` are both **deleted** (Module 3.8 — the Files page now reads real `qr_assets`). The only mock left is `src/lib/account/mock-data.ts` (`MOCK_PROFILE` — **known gap**, the real signed-in user's data isn't wired into the Account page yet, no master-prompt module explicitly owns this, see `docs/ARCHITECTURE.md`'s Module 3.1 section). Don't add new consumers of it — everything else now has real data.
- **`SUPABASE_ACCESS_TOKEN` is never persisted anywhere on disk** (confirmed again in Module 3.8 — no value in `.env.local`, no CLI credential file under `~/.supabase`). It's only ever a session-scoped shell env var, supplied ad hoc when a live migration push, ad-hoc query, or the `mailer_autoconfirm` toggle is genuinely needed. **Do not assume it's available going into a new session** — if a module's live verification needs a freshly confirmed test account, check early (try a real signup, see if it hits the email-confirmation gate) rather than discovering the gap mid-verification. If it's missing and genuinely needed, ask the user for it once, up front — this is a CLI personal access token, not the service-role key, and reusing it follows the pattern already established across Modules 3.1/3.6/3.7.
- **Temp-route technique gotcha (Module 3.5, reconfirmed useful through 3.8)**: a Route Handler folder prefixed with `_` (e.g. `_test-only-login`) is a Next.js "private folder" and is excluded from routing entirely — 404s even though the dev server compiles cleanly. Name any temporary verification route without a leading underscore, and always confirm via `git status` that it's deleted before committing.
- **Component architecture**: `QRTypeSelector`/`QRContentPanel` read from `qrTypeRegistry` (`src/lib/qr/registry.ts`) — never hardcode a QR type list. `DASHBOARD_NAV_ITEMS` is the single source for dashboard nav. `QrPlaceholderGraphic` is now only a fallback (invalid/empty content in `QRPreviewPanel`, or a dynamic QR pending its first save) — the QR detail page server-renders a real regenerated SVG. `QRCodeRowActions` (Module 3.5, extended Module 3.6) is the single implementation of Duplicate/Archive/Pause(dynamic only)/Delete/Download, used on both the list and the detail page (`showDownload` prop toggles the list-only quick-download button). `src/lib/analytics/aggregate.ts` holds pure, reusable event-aggregation functions, now fed real data (Module 3.7) via `listScanEvents()`.
- **Scan analytics internals (Module 3.7)**: `src/lib/qr/user-agent.ts`'s `parseUserAgent()` is the only place that should ever classify a User-Agent — order-sensitive (Edge/Opera before Chrome, iOS before macOS), don't duplicate this logic elsewhere. `QrScanEvent.countryCode` is `string | null` — genuinely absent (not "Unknown") when no edge geo header was present at scan time; `AnalyticsView` checks `events.some(hasCountry)` before rendering any country UI at all, so a new chart/filter that uses country must follow the same "only where the data genuinely supports it" pattern rather than assuming country is always populated. `listScanEvents()` (`src/lib/qr/queries.ts`) bounds its query to the last 30 days on purpose — don't widen this without also reconsidering whether `AnalyticsView`'s date-range filter should grow past `30d` first, or the two will disagree.
- **Dynamic-QR encoding rule (Module 3.6, don't regress)**: `resolveEncodedPayload(mode, qrType, content, slug)` (`src/lib/qr/render.ts`) is the _only_ function that should ever decide what gets encoded into a rendered QR image. A dynamic QR always encodes `buildRedirectUrl(slug)` (`src/lib/qr/redirect-url.ts`) and never the raw content — if you add a new place that renders a QR (preview, download, detail page, anywhere else), call this, not `buildQrPayload` directly, or dynamic QRs will silently start encoding the wrong thing again.
- **Redirect route internals (Module 3.6)**: `src/app/r/[slug]/route.ts` resolves via `resolve_qr_redirect` and records scans via `record_qr_scan` — both `SECURITY DEFINER` Postgres functions (`supabase/migrations/20260818120000_add_redirect_rpc_functions.sql`), called through the normal anon-key `createClient()`, **not** the service-role key (`SUPABASE_SERVICE_ROLE_KEY` is still blank and doesn't need to be filled in for this). `export const dynamic = "force-dynamic"` on that route is load-bearing — removing it risks the route being statically optimized and destination edits no longer propagating. Scan recording runs inside `after()` (`next/server`) so it never delays the redirect.
- **QR rendering (Modules 3.2–3.4, all complete)**: `src/lib/qr/render.ts` — `buildQrPayload()` (validate content against the type's Zod schema, then build), `slugifyForFilename()` (now the complete filename policy — Unicode-normalized, diacritics stripped, length-capped), plus the plain `renderQrSvg()`/`renderQrPngDataUrl()` kept only as Module 3.3's error fallback (don't call these directly from new UI — use the styled versions). `src/lib/qr/matrix.ts` (raw module matrix + finder-region geometry) and `src/lib/qr/styled-svg.ts` (`renderStyledQrSvg()`/`renderStyledQrPngDataUrl(payload, design, targetWidth)` — the real, fully-styled renderer everything should call; `targetWidth` is how the 512/1024/2048px presets work) are the current single sources of truth for turning content into a rendered QR. `src/lib/qr/reliability.ts` holds the contrast/logo-size/EC-level/quiet-zone rules — reuse these, don't reimplement. `src/lib/qr/logo.ts`'s `readLogoFile()` is the only place that should touch logo file input handling. **Module 3.5 note**: this whole pipeline is what "regenerate visuals from saved config" means — never store a rendered image in Postgres, store `design_config`/payload and call `renderStyledQrSvg`/`renderStyledQrPngDataUrl` again whenever a saved QR needs to be shown or downloaded.
- **jsdom gap, resolved**: `HTMLDialogElement.prototype.showModal`/`.close` are unimplemented in this project's jsdom version — polyfilled globally in `tests/setup.ts`.
- **Known, accepted Next.js limitation**: `notFound()` on this app returns HTTP 200, not 404, because the root `src/app/loading.tsx` creates a Suspense boundary above every route and streaming locks in the already-sent 200 status before `notFound()` can run. Documented in `docs/ARCHITECTURE.md` under Module 2.7. Not a regression to "fix" reflexively if it resurfaces elsewhere.
- **No charting library installed on purpose** (Module 2.8's charts are hand-rolled). Don't add one without a real reason.
- **Local Supabase via Docker still works** — `supabase/config.toml` exists, ports `54321`–`54329`. Useful for iterating on new migrations before `db push`ing them live. `supabase start` on an existing volume does **not** auto-apply new migrations — use `supabase db reset` after adding one.
- **The live project is now linked** (`supabase link`, Module 3.1) — `supabase/.temp/` holds the link state, gitignored via `supabase/.gitignore`. `SUPABASE_ACCESS_TOKEN` is not stored anywhere persistent (not in `.env.local`, not committed) — it was used interactively for the link/push/query calls in Module 3.1 and isn't needed again unless another live migration push or ad-hoc live query is required.
- **Live project auth config**: `mailer_autoconfirm=false` (real email confirmation required for signup — the "check your email" path in `SignupForm` is the one real users take), `disable_signup=false`, `uri_allow_list` includes `http://localhost:3000/**` (needed for `/auth/callback` redirects to be accepted — will need the production URL added too once one exists).
- RLS design intentionally leaves two gaps that are **not bugs**: `qr_codes` has no `anon` SELECT policy, `qr_scan_events` has no client-facing INSERT policy at all. Module 3.6 fills both gaps for the redirect path via two narrow `SECURITY DEFINER` RPC functions (see above) — **not** by adding an RLS policy, which would have reopened both gaps for every caller, not just the redirect route.
- On this machine, port 3000 is sometimes already in use by an unrelated project (`D:\AntiGravity\LMS`). Next.js auto-falls-back to 3001 — check the actual port before assuming. **Do not use a broad `taskkill /IM node.exe` (or equivalent kill-all-node-processes command) to stop a dev server** — it kills every Node process on the machine, including unrelated projects' dev servers if any are running. Find and stop only the specific PID for the server you started.
- Master build spec: [`QR_Code_Generator_Master_Build_Prompt.md`](../QR_Code_Generator_Master_Build_Prompt.md) at the repo root is the single source of truth for module order and acceptance criteria. Follow it exactly — do not merge or reorder Structure → UI → Features.
