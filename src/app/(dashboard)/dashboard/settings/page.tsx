import { RouteStub } from "@/components/layout/RouteStub";

export default function SettingsPage() {
  return (
    <RouteStub
      title="Settings"
      description="App settings that actually exist, added in Module 2.9."
    />
  );
}
