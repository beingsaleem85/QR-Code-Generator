import { RouteStub } from "@/components/layout/RouteStub";

export default function QrTypesPage() {
  return (
    <RouteStub
      title="QR Types"
      description="Overview of supported QR types, driven by the QR type registry (Module 1.3)."
    />
  );
}
