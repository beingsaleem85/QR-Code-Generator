import { RouteStub } from "@/components/layout/RouteStub";

export default function PricingPage() {
  return (
    <RouteStub
      title="Pricing"
      description="Placeholder — billing/monetization is out of scope until the core product is complete."
    />
  );
}
