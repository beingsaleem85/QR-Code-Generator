import { RouteStub } from "@/components/layout/RouteStub";

export default function PrivacyPage() {
  return (
    <RouteStub
      title="Privacy Policy"
      description="Production-ready legal copy is written and reviewed in Module 3.15 — must disclose dynamic-QR scan analytics (Module 3.7): timestamp, referrer, device type/OS/browser (parsed from User-Agent), and country when a hosting platform provides it. No raw IP, no visitor identifier, no location beyond country. Must also disclose feedback-QR submissions (Module 3.9): a visitor's rating/comment/optional contact field is stored only after they check a consent notice on the submission form itself; the QR owner alone can read their own QR's submissions, and a submission can never be edited or deleted through the app once made."
    />
  );
}
