import { RouteStub } from "@/components/layout/RouteStub";

export default function StaticQrPage() {
  return (
    <RouteStub
      title="Static QR Codes"
      description="Explains static QR codes and links into the generator."
    />
  );
}
